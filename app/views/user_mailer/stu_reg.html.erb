<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <% @email = EmailMessage.where(:email => "Register Student").first %>
  <body>
    <h1><%=@email.title%></h1>
    <p>
      <%=@email.header%><br>
    </p>
    <% if @user == nil
      @user = Student.first
      %>
      <%end%>
<table>
  <tbody>
      <tr>
        <td>Name:</td><td><%= @user.name %></td>
      </tr>
      <tr>
        <td>UIN:</td><td><%= @user.UIN %></td>
      </tr>
      <tr>
        <td>E-mail:</td><td><%= @user.email %></td>
      </tr>
      <tr>
        <td>US Citizen:</td><td><%= @user.US_Citizen %></td>
      </tr>
      <tr>
        <td>Degree Level:</td><td><%= @user.degree %></td>
      </tr>
      <tr>
        <td>Position Type:</td><td><%= @user.position_type %></td>
      </tr>
      <% $stu_slot[@user.id].each do |slot| %>
      <tr>
        <td><%= slot[0] %>:</td><td><%= slot[1] %></td>
      </tr>
      <% end %>

      

  </tbody>
</table>
    <p> Your chosen timeslots can be changed by selecting the link below:<br>
      <%= link_to 'Edit', @editlink %>
    </p>
    <p><%=@email.footer%></p>
  </body>
</html>
