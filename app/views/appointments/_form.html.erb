<%= form_for(@appointment) do |f| %>
    <% if @appointment.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

          <ul>
            <% @appointment.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :section %><br>
      <%= f.text_field :section, class: 'form-control no-radius width_300' %>
    </div>
    <div class="field">
      <%= f.label :time_slot %><br>
      <%= f.text_field :time_slot, class: 'form-control no-radius width_300' %>
    </div>
    <div class="field">
      <%= f.label :company %><br>
      <%= f.text_field :company, class: 'form-control no-radius width_300' %>
    </div>
    <div class="field">
      <%= f.label :student %><br>
      <%= f.text_field :student, class: 'form-control no-radius width_300' %>
    </div>
    <div class="field">
      <%= f.label :UIN, "UIN" %><br>
      <%= f.text_field :UIN, class: 'form-control no-radius width_300' %>
    </div>
    <div class="actions" >
      <%= f.submit "Submit" , class: "btn btn-primary no-radius" %>
    </div>
<% end %>
