<p id="notice"><%= notice %></p>

<div style = "text-align: center">
  <font size="5px" color=maroon><strong><%= "Events List"%></strong></font><br><br>
</div>

<div id="ABC1" align="center">
  <%= link_to 'New Event', new_event_path %>
  &nbsp&nbsp&nbsp|&nbsp&nbsp&nbsp
  <%= link_to 'Back', sessions_url %>
</div>
<br>

<table align="center" border="2px" cellspacing = "0px" bordercolor = "#000000" cellpadding = "4">
  <thead>
  <tr>
    <th>Event Date</th>
    <th>Event Name</th>
    <th>Timeslots</th>
    <th>Max Students</th>
    <th>Start Time</th>
    <th>End Time</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
      <tr>
        <td><%= event.event_date %></td>
        <td><%= event.name %></td>
        <td><table cellspacing="0px", border="0px">
          <% @event_slots[event.id].each do |slot| %>
              <tr>
                <td> <%= slot %></td>
              </tr>
          <% end %>
        </table></td>
        <td><%= event.max_students %></td>
        <td><%= event.start_time.strftime("%I:%M%p") %></td>
        <td><%= event.end_time.strftime("%I:%M%p") %></td>
        <td><%= link_to 'Show', event, class: "link-color" %></td>
        <td><%= link_to 'Edit', edit_event_path(event), class: "link-color" %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' }, class: "link-color" %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

